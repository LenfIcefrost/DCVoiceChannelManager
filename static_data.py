from dataclasses import dataclass, field

@dataclass()
class bot_setting:
    bot_token = ""

    def to_save_data(self) -> str:
        return f""

@dataclass()
class administrator:
    users_id = [
        
    ]

    def to_save_data(self) -> str:
        return f""
